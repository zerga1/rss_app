<div id="recent_episodes">
  <h3>NSO.hu hírek</h3>
  <%= link_to "Frissítés", update_path%>
  <ul>
  <% for entry in FeedEntry.all(:limit => 20, :order => "published_at desc") %>
    <li><%= link_to h(entry.name), entry.url %></li>
    <%= entry.summary %>
  <% end %>
  </ul>
</div>